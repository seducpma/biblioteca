<% form_for @ambiente, :action => "create_ambiente" do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :user_id, "Usuário" %><br />
    <%= current_user.login %>
    <%= f.hidden_field(:user_id, :value => current_user.id) %>
  </p>
  <p>
    <%= f.label :turma %><br />
    <%= f.collection_select(:turma, @classes, :id_classe, :classe_descricao, :include_blank => ' -- Selecionar -- '  ) %>
  </p>
  <p>
    <%= f.label :data %><br />
    <%= f.text_field :data, :size => 5 %>
  </p>
  <p><%= f.submit %></p>
<% end %>
